# QuiConvert Source

Baseline: QuiConvert Plugin 5.1.3 Stable

## Structure

- `frontend-plugin/` — WordPress plugin source
- `backend/` — Flask backend source
- `docs/` — ADR, Release Notes, Roadmap
- `qa/` — QA Suite and test reports
- `scripts/` — build/release scripts

## Current stable plugin

QuiConvert Plugin 5.1.3  
QC Runtime 0.8.0
