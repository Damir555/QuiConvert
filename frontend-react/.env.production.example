# Copy this file to .env.production only when building for deployment.
# Replace the example host if the backend is deployed at another address.
VITE_PDF_API_BASE=https://quiconvert-backend.onrender.com/api/pdf
